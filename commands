commands

// Local enviourment

npm run start -> to start local dev server (to compile sass to css)
open browser -> http://localhost:9090



// For Git

git status -> to check the modified/added files

git add <file-name> -> to add specific file

git add . -> to add all modified files

git comment -m "message" -> to commit all the changes so that we can push to cloud

git push origin <branch-name> -> to push changes to cloud